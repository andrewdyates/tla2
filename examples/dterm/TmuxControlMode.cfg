SPECIFICATION Spec

\* Parser state constants - using model values
CONSTANTS
    Idle = Idle
    InBlock = InBlock
    Broken = Broken

    Inactive = Inactive
    Active = Active

    BeginLine = BeginLine
    EndLine = EndLine
    ErrorLine = ErrorLine
    DataLine = DataLine
    NotificationLine = NotificationLine

    SessionChanged = SessionChanged
    Exit = Exit
    Output = Output
    WindowAdd = WindowAdd
    WindowClose = WindowClose
    Other = Other

    \* Bounded values for model checking
    MAX_SESSION_ID = 2
    MAX_WINDOW_ID = 2
    MAX_PANE_ID = 2
    MAX_CMD_NUM = 3
    MAX_TIMESTAMP = 2
    MAX_PENDING_BLOCKS = 2

\* Invariants to check
INVARIANT TypeInvariant
INVARIANT SafetyInvariant
