\* Configuration for ParallelModelCheckerTest.tla
\* All constants are defined in the module

SPECIFICATION Spec

\* Disable deadlock checking - stuttering semantics allows infinite stuttering
\* TLA2 correctly handles this, but TLC needs explicit flag
CHECK_DEADLOCK FALSE

INVARIANT TypeOK
INVARIANT WorkAccountingCorrect
INVARIANT ActiveWorkersBounded

\* CompleteExploration is a state-level formula - check as invariant
\* (TLC warns when using state formulas as PROPERTY)
INVARIANT CompleteExploration
