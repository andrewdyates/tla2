SPECIFICATION Spec

CONSTANTS
    Workers = {1, 2}
    CachingEnabled = TRUE

INVARIANT TypeOK
INVARIANT NoDoubleExploration

\* This property SHOULD be violated when caching is buggy:
\* The cache can return wrong values causing false positives
INVARIANT NoFalsePositives
