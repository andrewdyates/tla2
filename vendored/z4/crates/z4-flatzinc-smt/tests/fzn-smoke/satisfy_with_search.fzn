% Satisfaction problem with search annotations (FD track).
% Used by cli_smoke.rs to test auto-activation of branching search.
var 1..4: x :: output_var;
var 1..4: y :: output_var;
constraint int_ne(x, y);
solve :: int_search([x, y], first_fail, indomain_min, complete) satisfy;
